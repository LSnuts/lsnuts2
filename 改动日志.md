# 改动日志

项目最近改动按时间倒序记录。时间均为 Asia/Shanghai（北京时间）。

## 2026-08-06 14:15

- 优化 `start.ps1` 和 `stop.ps1`：使用 PID 文件管理项目自己的后端与 Cloudflare Tunnel 进程。
- 修复停止脚本误杀其他 Python 进程的问题。
- 增加启动前依赖检查、PostgreSQL 状态检查、后端 5000 端口等待和失败日志路径提示。
- 增加后端和 Tunnel 日志输出到 `instance/logs/`。
- 增加 `-KeepPostgres` 参数，可在停止应用时保留 PostgreSQL 服务。
- PowerShell 语法检查通过。

## 2026-08-06 14:12

- 修复 `backend/manage.py` 与 Flask-Migrate 4.x 的兼容问题。
- `python manage.py upgrade` 现在可以正常执行。

## 2026-08-06 14:06

- 修复 Python 3.13 与 eventlet 的兼容问题。
- SocketIO 改用 `threading` 模式，不再依赖 eventlet。
- 更新 `backend/requirements.txt`，移除 eventlet 固定依赖。

## 2026-08-06 14:00

- 本地后端正式切换到 PostgreSQL `garden1` 数据库。
- 配置地址为 `localhost:5432`，数据库用户为 `lsnuts`。
- 确认 PostgreSQL 中原有 6 个用户数据仍然存在。
- 修复因后端误使用 SQLite 导致登录提示“用户未注册”的问题。

## 2026-08-06 13:55

- 对现有 PostgreSQL 数据库执行结构升级，没有迁移、复制或覆盖用户数据。
- 补充 `users.email` 字段。
- 补充 `posts.is_hidden` 字段。
- 补充 `files.file_size` 字段。
- 增加消息和通知查询索引。
- 更新 Alembic 数据库版本。

## 2026-08-06 13:30

- 管理员界面新增用户保密邮箱修改功能。
- 管理员用户列表增加邮箱脱敏显示。
- 增加用户名、邮箱和账号码搜索。
- 用户操作改为下拉菜单，优化表格布局和移动端显示。
- 后端增加邮箱格式校验和邮箱唯一性校验。

## 2026-08-06 13:24

- 正式接入网易 163 SMTP 邮箱。
- 注册流程增加邮箱字段。
- 找回密码流程改为向用户邮箱发送一次性重置链接。
- 重置链接有效期为 1 小时，使用后立即失效。
- SMTP 配置写入本地 `backend/.env`，该文件已加入 Git 忽略规则。
- SMTP 登录验证成功。

## 2026-08-06 12:50

- 完成项目第一轮安全与性能优化。
- 增加登录、找回密码和 Token 校验限流。
- 优化通知接口，减少重复数据库查询。
- 增加分页参数限制。
- 网盘统计改为读取数据库保存的文件大小。
- 统一前端头像资源地址处理。
- 前端生产构建验证通过。
